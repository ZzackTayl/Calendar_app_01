name: calendar_server
description: A Serverpod server for the calendar mobile application

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  serverpod: ^2.0.0
  serverpod_serialization: ^2.0.0

dev_dependencies:
  serverpod_cli: ^2.0.0
  test: ^1.21.0

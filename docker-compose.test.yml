services:
  # Test PostgreSQL database
  test-db:
    image: postgres:15-alpine
    container_name: polyharmony-test-db
    env_file:
      - ${TEST_ENV_FILE:-config/testing/.env.testing.generated}
    environment:
      POSTGRES_DB: polyharmony_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${TEST_DB_PASSWORD:?TEST_DB_PASSWORD must be provided}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5433:5432"  # Use different port to avoid conflicts with dev
    volumes:
      - test_db_data:/var/lib/postgresql/data
      - ./tests/db/init:/docker-entrypoint-initdb.d  # Initialization scripts
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d polyharmony_test"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - test-network

  # Test application for integration testing
  app-test:
    build:
      context: .
      dockerfile: Dockerfile.test
      target: integration
    container_name: polyharmony-app-test
    env_file:
      - ${TEST_ENV_FILE:-config/testing/.env.testing.generated}
    environment:
      - NODE_ENV=test
      - NEXT_TELEMETRY_DISABLED=1
      # Test database connection
      - DATABASE_URL=${DATABASE_URL:?DATABASE_URL must be provided}
      # Test-specific Supabase configuration
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL:?NEXT_PUBLIC_SUPABASE_URL must be provided}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY:?NEXT_PUBLIC_SUPABASE_ANON_KEY must be provided}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY:?SUPABASE_SERVICE_ROLE_KEY must be provided}
      # Test encryption key
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:?ENCRYPTION_KEY must be provided}
      # Disable external services in tests
      - DISABLE_EXTERNAL_APIS=true
    ports:
      - "3001:3000"  # Use different port for test app
    depends_on:
      test-db:
        condition: service_healthy
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    networks:
      - test-network
    profiles:
      - integration-tests  # Only start when running integration tests

  # Supabase local instance for testing
  supabase-test:
    image: supabase/postgres:15.1.0.147
    container_name: polyharmony-supabase-test
    env_file:
      - ${TEST_ENV_FILE:-config/testing/.env.testing.generated}
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${SUPABASE_DB_PASSWORD:?SUPABASE_DB_PASSWORD must be provided}
      POSTGRES_PORT: 5432
    ports:
      - "54322:5432"
    volumes:
      - supabase_test_data:/var/lib/postgresql/data
      - ./supabase/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - test-network
    profiles:
      - supabase-tests  # Only start when testing with full Supabase

networks:
  test-network:
    name: polyharmony-test-network
    driver: bridge

volumes:
  test_db_data:
    name: polyharmony-test-db-data
  supabase_test_data:
    name: polyharmony-supabase-test-data

# Override for CI/CD environments
# Usage: docker-compose -f docker-compose.test.yml -f docker-compose.test.ci.yml up
x-ci-overrides: &ci-overrides
  test-db:
    # Use in-memory storage for faster CI tests
    tmpfs:
      - /var/lib/postgresql/data:noexec,nosuid,size=1G
    volumes: []
  app-test:
    # Reduce resource usage in CI
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1'

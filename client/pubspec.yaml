name: calendar_client
description: A Serverpod client for the calendar mobile application

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  serverpod_client: ^2.0.0
  serverpod_serialization: ^2.0.0

dev_dependencies:
  test: ^1.21.0